apiVersion: v1
kind: Service
metadata:
  name: webapp-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: webapp-frontend
  ports:
    - port: 80      # Puerto externo por el que se accederá al servicio
      targetPort: 80   # Puerto interno del contenedor al que redirige
