apiVersion: v1
kind: Service
metadata:
  name: events-microservice-service
spec:
  type: ClusterIP
  selector:
    app: events-microservice
  ports:
    - port: 6001         # Puerto del servicio dentro del clúster
      targetPort: 6001   # Puerto del contenedor